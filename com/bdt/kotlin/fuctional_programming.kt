package com.bdt.kotlin

/*fun main(){
    // seperti yang sudah disampaikan di awal akademi. Kotlin adalah sebuah multiparadigm programming
    // language. Artinya selain merupakan bahasa pemrograman berorientasi objek, dalam penulisannya
    // sintaksnya Kotling menggunakan gaya functional protgramming.
    // Contoh
    //val list = getListUser()

}*/
/*val list = getListUser()
fun getListUser(): List<String>{
    val name = mutableListOf<String>()
    for (user in list){
        name.add(user.name)
    }
    return name
}
// Kode diatas biasanya kita tuliskan untuk mendapatkan nilai tertentu dari sebuah list. Karena kode
// pada Kotlin bisa dituliskan dengan gaya fungsional, maka kode di atas cukup dituliskan seperti berikut

fun getUserName(): List<String>{
    return list.map{
        it.name
    }
}*/